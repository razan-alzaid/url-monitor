server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /add {
        proxy_pass http://api:5000;
    }

    location /urls {
        proxy_pass http://api:5000;
    }
}